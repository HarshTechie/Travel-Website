{
  "name": "travelwebsite",
  "version": "0.1.0",
  "private": true,
  "description": "Root orchestrator for client (React) and server (Express).",
  "scripts": {
    "install:all": "npm install && npm --prefix client install && npm --prefix server install",
    "client": "npm --prefix client start",
    "server": "npm --prefix server start",
    "dev": "concurrently -n client,server -c blue,green \"npm --prefix client start\" \"npm --prefix server start\"",
    "build": "npm --prefix client run build",
    "seed": "npm --prefix server run seed"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
